# שלב 2 — נראות פר־צד לקישורים

מסמך חוזה. שלב 1 (‏`752b5dc`) מדכא `link_coverage` לציטוטי פרק/פרשה שלמים ומשאיר
את הקישור מוצג **פעם אחת** בראש היחידה. שלב 2 מסיר גם את ההופעה הזאת ומגיע לזהות
מלאה עם ספריא — אבל רק אם מקור־האמת עובר ל־SefariaExport ושלושה צרכנים משתנים יחד.

## עקרון־על: היצואן מחליט, לא הגנרטור

היום `SefariaWholeUnitRefs` **משחזר** מדיניות תצוגה של ספריא מתוך schema חלקי.
זה עובד לקורפוס הנוכחי, אבל שביר: ה־proxy של הפרשות מזהה מבנה בשם `Parasha`,
בעוד ספריא משתמשת ב־`TermSet({"scheme": "Parasha"})` והתאמת כותרות; שינוי סמנטי
תקין יעבור את כל ה־invariants המקומיים וייצר סט שונה בשקט.

ב־[`run_exports.py:233-248`](../SefariaExport/run_exports.py) כבר נוצרים `oref1`/`oref2`
לכל שורת קישור, בדיוק לפני כתיבתה. שם — ורק שם — זמינים יחד ה־TermSet, מבני
הספרים, `index_node.depth`, מספר ה־sections בכל צד, ושתי הקבוצות הרשמיות. לכן
**ההחלטה תחושב שם ותיוצא פר־צד**, והגנרטור רק יתרגם אותה לצד המאוחסן.

## חוזה הייצוא

### שני שדות חדשים ב־CSV של הקישורים

```text
Suppression Mask 1
Suppression Mask 2
```

ערכי ביט, מחושבים מהפונקציות הרשמיות של ספריא:

```text
1 = צד העוגן אינו ברמת סגמנט        (len(anchor.sections) != node_depth)
2 = הצד השני גס מדי                  (len(other.sections) + 1 < node_depth)
4 = פרק תלמוד/משנה/תוספתא שלם        (get_talmud_perek_ref_set())
8 = פרשה שלמה                        (get_parasha_ref_set())
```

**mask ולא סיבה יחידה**: אותו צד יכול לענות ליותר מתנאי אחד.

### `metadata/link-visibility-v1.json`

לא מקור ההחלטה — כלי QA. מכיל: גרסת פורמט, שתי הקבוצות ממוינות, ספירות ו־hashes,
ו**־SHA של `Sefaria-Project` ששימש בפועל**. מאפשר לעקוב אחרי מקור ההחלטה
ולשחזר איזו גרסת מדיניות יצרה כל mask.

### תלות: קיבוע גרסת ספריא

`05_clone_sefaria_project.sh` עושה `git clone --depth 1` של ברירת המחדל, בלי SHA
ובלי לתעד אותו; `release_metadata.source_commit` הוא commit של SefariaExport, לא
של Sefaria-Project. **חייבים לפחות לתעד את ה־SHA בארכיון**, ועדיף לאפשר pin מפורש —
אחרת ה־masks אינם ניתנים לשחזור.

## כלל המיזוג — הקריטי ביותר

`linkId` נגזר מ־`(sourceLineId, targetLineId, connectionTypeId)`
(`SefariaLinksImporter.kt:478`), ולכן **כמה שורות CSV מתמזגות לקישור אחד**.

נמדד על בניית ה־QA (‏`link-import-metrics.json`):

| type | resolvedPairs | written | מוזגו |
|---|---|---|---|
| OTHER | 1,668,564 | 1,590,554 | **78,010 (4.7%)** |
| COMMENTARY | 1,886,181 | 1,852,182 | 33,999 (1.8%) |
| EIN_MISHPAT | 140,352 | 137,620 | 2,732 |

~115K זוגות מתמזגים, ‏78K מהם ב־`OTHER` — בדיוק הסוג שהדיכוי מכוון אליו.

> **הכלל:** צד של קישור מאוחד מדוכא **רק אם כל** שורות המקור שתרמו לאותו
> `(linkId, side)` מדוכאות. תרומה מוצגת אחת מספיקה כדי להשאיר את הצד מוצג.

מימוש: לצבור בנפרד `hasVisible = OR(mask == 0)` ו־`reasonMask = OR(mask)` פר
`(linkId, side)`. כותבים שורה רק כאשר `hasVisible = false`; הסיבות אינן צריכות
להיות זהות כדי שכל התרומות יהיו מדוכאות. הצבירה נעשית ב־SQLite זמני כדי לא
להחזיק מיליוני מפתחות boxed ב־heap.

## הסכמה

```sql
CREATE TABLE IF NOT EXISTS link_suppressed_side (
    linkId     INTEGER NOT NULL,
    side       INTEGER NOT NULL,      -- 0 = צד המקור השמור, 1 = היעד
    reasonMask INTEGER NOT NULL,      -- OR של סיבות התרומות המדוכאות; מעולם לא 0
    PRIMARY KEY (linkId, side),
    FOREIGN KEY (linkId) REFERENCES link(id) ON DELETE CASCADE
);
```

מספר השורות תלוי בכל ארבעת מסנני ספריא ובמיזוג שורות ה־CSV לאותו `linkId`, ולכן
נמדד בכל ייצוא ואינו מקובע לאומדן של שלב 1. **אין אינדקס נוסף** — ה־PK כבר נותן
`linkId` כ־left prefix.

## דו־כיווניות: מטריצה מלאה, לא רשימה

אין לקודד `OTHER/REFERENCE/RELATED` רק מפני שאלה הסוגים שנמדדו. יש למפות את **כל**
`ConnectionType` לשלוש קטגוריות: תלוי־טקסט (כבר נראה הפוך), הפניה (צריך
דו־כיווניות), חד־כיווני. כל ארבעת מסלולי השאילתה
(`database_library_provider.dart` — 315, 365, 549, 764) חייבים לצרוך **קבוע משותף
אחד**, אחרת ייווצר drift נוסף.

## סדר ביצוע

מיזוג הגנרטור אחרון — לא בגלל הורדה מלאה (‏`CREATE TABLE` נשלח כמיגרציה ב־patch,
ומשתמש מעודכן יקבל דלתא), אלא בגלל **תאימות לאחור**: updater ישן זורק ב־
`hashTableOrderForSchemaVersion` עוד לפני ההחלה. לכן צריכה להיות **מדיניות שחרור
מפורשת** — אחוז אימוץ מינימלי או חלון זמן — ולא המתנה לא מוגדרת.

1. `SefariaExport`: שני ה־masks, קובץ metadata מאומת, SHA חובה של ספריא, מוני QA לפי סיבה וצד.
2. ייצוא אמיתי + ניתוח התנגשויות `linkId` על נתונים חיים.
3. רק אז לסגור סופית את סכמת `link_suppressed_side`.
4. `seforim_library_updater`: הטבלה בשתי הרשימות, `case 3` ב־`hashTableOrderForSchemaVersion`, שמירת סדר סכמה 2.
5. `otzaria`: מטריצת סוגים, דו־כיווניות **מגודרת בקיום הטבלה**, סינון הצד המדוכא.
6. שחרור האפליקציה + מדיניות שחרור.
7. מיזוג הגנרטור ופרסום DB בסכמה 3.

## שערי אימות

1. `allTests` ירוק (רץ גם על push ל־`otzaria` מאז `d98bed7`).
2. delta עם `baseline_ref` טרום־שלב־2 → ה־patch מכיל את המיגרציה ואת `upsert_link_suppressed_side`.
3. `producePatchAndVerify` — `prev + patch == new`.
4. החלה באפלייר האמיתי **ב־WAL** (האפליקציה מעבירה ל־WAL ומחזירה אחריה), כולל שיא גודל WAL. **טרם נמדד.**
5. אפליקציה חדשה על DB ישן: אין דו־כיווניות, אין רגרסיה.
6. אפליקציה ישנה על DB חדש: דחייה נקייה, לא שחיתות.
7. אימות מוני ה־metadata מול כל ערכי ה־mask ב־CSV, לפי צד וסיבת דיכוי. אין שער
   שוויון מול ה־proxy הישן: הוא נשאר רק למסלול legacy של סכמה 2 ואינו מקור סמכות.

## מה שלב 2 **לא** משנה

מזהי קישורים, כיוון האחסון, `link_range`, ותוכן הטקסט. ‏`link_coverage` נשאר כפי
שהוא אחרי שלב 1.
